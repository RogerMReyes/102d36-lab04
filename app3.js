function messageToGuest(guest, answer) {
    if(answer == true){
        document.write("Make sure to brink some hamburgers with you next time " + guest);
    } else {
        document.write("I'm sure there's a type of food out there for you somewhere! " + guest);
    }
}

